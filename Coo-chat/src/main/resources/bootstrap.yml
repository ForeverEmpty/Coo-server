spring:
  application:
    name: Coo-chat
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848 # Nacos 地址
        file-extension: yaml        # 对应 Data ID 的后缀
        group: DEFAULT_GROUP
        shared-configs:
          - data-id: Coo-auth.yaml
            group: DEFAULT_GROUP
            refresh: true
      discovery:
        server-addr: 127.0.0.1:8848 # 之前的服务发现配置也可以移到这里
        port: 9090